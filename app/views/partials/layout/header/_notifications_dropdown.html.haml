- if current_user.present?
  .notifications-dropdown-menu.absolute.top-full.-right-5.py-4.px-6.hidden.z-10
    - if current_user.notifications.blank?
      .bg-primary-500.rounded-b-lg.py-4.w-40.text-center
        No notifications
    - else
      %ul.w-96.py-2.bg-primary-500.flex.flex-col.rounded-b-lg
        - current_user.notifications.newest_first.limit(5).each do |notification|
          = render partial: "user/profiles/notification", locals: {notification: notification, transparent: true}